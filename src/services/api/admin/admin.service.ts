import { apiRequest } from '../../apiClient'
import type {
  MetricsUserResponse,
  PaginatedUsersResponse,
  SystemMetricsResponse,
  PaginatedErrorLogsResponse,
} from './admin.models'

export const adminService = {
  // Deprecated AdminControler endpoints removed.
  // Use adminPlansService for plan-related tasks.
}
